# from cryptography.fernet import Fernet
# key = Fernet.generate_key()
#
# convert_datatype = Fernet(key)
# #Encrpy
# a = convert_datatype.encrypt(bytes("Hello World","ASCII"))
# print(a)
# # decrypt a
# key_clone = key
# convert_datatype_clone = Fernet(keyvs_clone)
# b = convert_datatype_clone.decrypt(a)
# print(b)

